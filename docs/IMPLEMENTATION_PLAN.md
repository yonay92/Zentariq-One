# IMPLEMENTATION_PLAN.md

# Zentariq One — Detailed Sprint Implementation Plan

Version: 1.0
Generated by: Lead Software Architect
Status: Approved for Execution — Awaiting Yonay Approval

---

## Dependency Chain

```
Phase 0 (Foundation)
  ↓
Sprint 1 (Auth + SaaS Core)
  ↓
Sprint 2 (Studies + Visit Templates)
  ↓
Sprint 3 (Subjects)
  ↓
Sprint 4 (Visits + Calendar)
  ↓
Sprint 5 (Charts + Data Entry)
  ↓
Sprint 6 (Regulatory)
  ↓
Sprint 7 (Enterprise Document Center)
  ↓
Sprint 8 (Task Engine)
  ↓
Sprint 9 (Business Rules Engine)
  ↓
Sprint 10 (Clinical Intelligence)
  ↓
Sprint 11 (Analytics)
  ↓
Sprint 12 (Settings)
  ↓
Sprint 13 (Security Hardening)
  ↓
Sprint 14 (Production Readiness)
```

No sprint may begin until the previous sprint's Definition of Done is satisfied.

---

## Phase 0 — Project Foundation

**Goal:** Running, deployable Next.js skeleton with Supabase connection and empty authentication.

### Deliverables

- GitHub repository created
- Next.js app scaffolded with `pnpm create next-app`
- TypeScript configured (strict mode)
- Tailwind CSS configured
- ESLint + Prettier configured
- `.env.local` with Supabase credentials
- Supabase project created
- CI/CD pipeline (GitHub → Vercel auto-deploy on main)
- Empty `/app/(auth)/login` page rendering

### Files Created

```
next.config.ts
tsconfig.json
tailwind.config.ts
eslint.config.mjs
.prettierrc
package.json
.env.local
.env.example
```

### Definition of Done

- `pnpm dev` runs without errors
- `pnpm build` succeeds
- Vercel deployment succeeds
- Supabase connection is verified

---

## Sprint 1 — Authentication & SaaS Foundation

**Goal:** Multi-tenant authentication with company isolation, roles, permissions, and site access.

### Database Migrations

```
001_companies_sites_users.sql
  - companies
  - company_settings
  - company_modules
  - profiles (mapped to auth.users)
  - sites
  - user_sites
  - user_invitations           ← (GAP-DB-02)

002_roles_permissions.sql
  - roles
  - permissions
  - user_roles
  - role_permissions
  - composite unique constraints  ← (GAP-DB-07)
```

### SQL Functions

```sql
current_company_id()
current_user_sites()
has_permission(permission_key text)
can_access_site(site_id uuid)
```

### RLS Policies

- `companies` — users read own company only
- `profiles` — users read same-company profiles only
- `sites` — users read authorized sites only
- `roles` — users read same-company roles only
- `user_sites` — admin or self only

### Seed Data

```
001_roles.sql     → admin, ceo, crc, data_entry, regulatory, pi
002_permissions.sql → all permission keys from SECURITY.md §6
```

### Services

```
AuthService          — login, logout, session, current user
CompanyService       — current company, settings
UserService          — CRUD, invite, role assign, site assign
PermissionService    — hasPermission, canAccessSite
SiteService          — CRUD sites, user assignments
```

### API Routes

```
POST   /api/auth/session
POST   /api/users/invite
GET    /api/users
PATCH  /api/users/:id
GET    /api/company/current
PATCH  /api/company/settings
GET    /api/sites
POST   /api/sites
PATCH  /api/sites/:id
```

### UI Pages

```
/app/(auth)/login/page.tsx
/app/(auth)/invite/[token]/page.tsx
/app/(auth)/reset-password/page.tsx
/app/(app)/layout.tsx         → AppShell (Sidebar + TopBar)
/app/(app)/settings/users/page.tsx
/app/(app)/settings/roles/page.tsx
/app/(app)/settings/sites/page.tsx
/app/(app)/settings/company/page.tsx
```

### Components to Build

```
AppShell, Sidebar, TopBar, PageHeader
Button, Card, Badge, TextInput, SelectInput
DataTable, FilterBar, TableActionMenu
Modal, Drawer, Toast, LoadingSpinner, EmptyState
```

### Tests

```
unit:  PermissionService.test.ts
int:   company-isolation.test.ts, site-isolation.test.ts
e2e:   auth.spec.ts (login, logout, invite flow)
```

### Definition of Done

- Admin can log in and log out
- Admin can invite users with roles and site access
- User accepts invitation via email link
- Site access filtering works (user A cannot see site B data)
- Company isolation works (company A cannot see company B)
- Audit Trail records: login, user creation, permission changes
- RLS tested: cross-company query returns 0 rows

---

## Sprint 2 — Study Management

**Goal:** Complete study lifecycle from manual creation to AI-assisted protocol extraction.

### Database Migrations

```
003_studies_visit_templates.sql
  - studies
  - study_sites
  - study_staff
  - visit_templates
  - visit_template_items
  - study_documents
  - study_ai_extractions
  - study_document_requirements
```

### Storage Buckets

```
protocols    → private, signed URLs, 1 hour expiry
studies      → private
```

### Edge Functions

```
supabase/edge-functions/protocol-ai/index.ts
  → Receives: file_id, study_id
  → Calls: Claude claude-sonnet-4-6 with Protocol Agent prompt
  → Stores result in: study_ai_extractions
  → Returns: extraction_id
```

### Services

```
StudyService
  - createStudy(data)
  - uploadProtocol(study_id, file)
  - approveAIExtraction(extraction_id)
  - assignSites(study_id, site_ids)
  - activateStudy(study_id)
  - closeStudy(study_id)

VisitTemplateService
  - createTemplate(study_id, items)
  - approveTemplate(template_id)
  - archiveTemplate(template_id)
```

### API Routes

```
GET    /api/studies
POST   /api/studies
POST   /api/studies/from-protocol
POST   /api/studies/:id/approve-ai-extraction
PATCH  /api/studies/:id
POST   /api/studies/:id/close
GET    /api/studies/:study_id/visit-templates
POST   /api/studies/:study_id/visit-templates
POST   /api/visit-templates/:id/approve
POST   /api/visit-templates/:id/archive
```

### UI Pages

```
/app/(app)/studies/page.tsx           → Study list with filters
/app/(app)/studies/new/page.tsx       → Manual create form
/app/(app)/studies/[id]/page.tsx      → Study profile (tabs)
/app/(app)/studies/[id]/visit-templates/page.tsx → Template builder
```

### Key UI Components

```
StudyProfileHeader
VisitTemplateBuilder (drag-reorder visit items)
AIExtractionReview (show extracted fields with confidence)
ProtocolUploader (file drop zone + progress)
AIReviewPanel
```

### Tests

```
unit:   StudyService.test.ts, VisitTemplateService.test.ts
int:    study-creation.test.ts
e2e:    study-creation.spec.ts
```

### Definition of Done

- Admin can create study manually
- Admin can upload protocol → AI extracts draft study
- Admin reviews and approves AI extraction
- Visit template builder works (add/remove/reorder visits)
- Template requires approval before subjects can be created
- Protocol amendments create new template versions (old version preserved)
- Audit Trail: study created, protocol uploaded, AI approved

---

## Sprint 3 — Subject Management

**Goal:** Complete subject lifecycle from creation through status management and timeline.

### Database Migrations

```
004_subjects_visits_calendar.sql  (partial — subjects and timeline)
  - subjects
    → UNIQUE(study_id, subject_number) ← (GAP-REQ-02)
  - subject_status_history
  - subject_notes
  - subject_documents
  - subject_milestones
  - subject_timeline
```

### Services

```
SubjectService
  - createSubject(data)          → validates study active, site assigned, template approved
  - changeStatus(subject_id, status, reason)
  - addNote(subject_id, note)
  - addMilestone(subject_id, milestone_type, date)
  - getProfile(subject_id)       → all nested data per tab (lazy by tab)
  - linkDocument(subject_id, file_id)
```

### Business Rule Integrations at Subject Creation

```
1. Validate study status = 'active'
2. Validate study_site relationship exists
3. Validate approved visit template exists  ← (GAP-REQ-03)
4. Create subject record
5. Generate visit schedule (calls VisitService.generateFromTemplate)
6. Create calendar events for each visit
7. Write subject_timeline entry: "Subject created"
8. Write audit_log
9. Recalculate analytics KPI: active_subjects
```

### API Routes

```
GET    /api/subjects
POST   /api/subjects
GET    /api/subjects/:id
PATCH  /api/subjects/:id
POST   /api/subjects/:id/status
POST   /api/subjects/:id/notes
POST   /api/subjects/:id/documents
```

### UI Pages

```
/app/(app)/subjects/page.tsx           → Subject list
/app/(app)/subjects/new/page.tsx       → Create form
/app/(app)/subjects/[id]/page.tsx      → Profile (tabbed)
/app/(app)/subjects/[id]/timeline/page.tsx
/app/(app)/subjects/[id]/notes/page.tsx
/app/(app)/subjects/[id]/documents/page.tsx
/app/(app)/subjects/[id]/history/page.tsx
```

### Tests

```
unit:   SubjectService.test.ts (status flow, validation)
int:    subject-workflow.test.ts
e2e:    subject-workflow.spec.ts
```

### Definition of Done

- CRC can create subject (validates study/site/template)
- Subject number unique per study (DB constraint enforced)
- Status flow: Pre-Screening → Screening → Randomized → Active → Completed
- Each status change writes: subject_status_history, subject_timeline, audit_log
- Early termination cancels future visits
- Timeline visible on subject profile

---

## Sprint 4 — Visits & Calendar

**Goal:** Automatic visit generation, calendar views, visit completion workflow.

### Database Migrations (completing 004)

```
  - visits (completes 004_subjects_visits_calendar.sql)
    → NULL constraint: visit_template_item_id nullable (for unscheduled)
  - visit_history
  - visit_notes
  - calendar_events
```

### Services

```
VisitService
  - generateFromTemplate(subject_id, template_id, baseline_date)
  - completeVisit(visit_id)    → creates Chart + Task via BusinessRuleEngine
  - rescheduleVisit(visit_id, new_date, apply_to)
  - cancelVisit(visit_id, reason)
  - createUnscheduled(subject_id, visit_date, reason)
  - checkWindowStatus(visit_id, completion_date)

CalendarService
  - getEvents(site_id, date_range, types)
  - createManualEvent(data)
  - updateEvent(event_id, data)
  - cancelEvent(event_id)
  - syncFromVisit(visit_id)    → keeps calendar_event in sync with visit
```

### Business Rules at Visit Completion

```
Trigger: visits.status = 'completed'
Actions:
1. Check if completion_date is within visit window
2. If out of window: mark visit out_of_window, increase chart priority
3. Create chart record
4. Create Data Entry task (via TaskService)
5. Update subject_timeline: "Visit {name} completed"
6. Recalculate analytics: visits_completed, pending_charts
7. Write audit_log
8. Notify CRC if out of window
```

### API Routes

```
GET    /api/visits
POST   /api/visits/unscheduled
PATCH  /api/visits/:id/status
POST   /api/visits/:id/reschedule
POST   /api/visits/:id/complete
GET    /api/calendar/events
POST   /api/calendar/events
PATCH  /api/calendar/events/:id
DELETE /api/calendar/events/:id
```

### UI Pages

```
/app/(app)/calendar/page.tsx          → Month/Week/Day views
/app/(app)/visits/page.tsx            → Visit list (filterable)
/app/(app)/visits/[id]/page.tsx       → Visit detail
```

### Tests

```
unit:   VisitService.test.ts (window calculation, rescheduling)
int:    visit-completion.test.ts
e2e:    visit-completion.spec.ts
```

### Definition of Done

- Subject creation auto-generates all scheduled visits
- Calendar shows patient visits and manual events
- CRC can complete visit → chart and task auto-created
- Out-of-window visits marked and priority elevated
- Rescheduling options work (only this / all future)
- Cancelled visits preserved (not deleted)
- Unscheduled visits work correctly

---

## Sprint 5 — Charts & Data Entry

**Goal:** Chart queue with priority ordering, Data Entry workflow, EDC completion.

### Database Migrations

```
005_charts_tasks_analytics.sql (partial — charts)
  - charts
    → UNIQUE(visit_id)  ← (GAP-REQ-01)
    → priority_score numeric (computed column for sorting)
  - chart_history
  - chart_comments
  - chart_metrics
  - chart_assignments
```

### SQL Functions

```sql
calculate_chart_priority(chart_id uuid) → priority_score numeric
  Rules:
  - +100: sponsor visit approaching (within 7 days)
  - +80:  > 7 days overdue
  - +60:  out of window
  - +40:  4-7 days overdue
  - +20:  1-3 days overdue
  - +0:   default
```

### Services

```
ChartService
  - createFromVisit(visit_id)    → called by VisitService after completion
  - getQueue(site_id, filters)   → ordered by priority_score DESC
  - markEntered(chart_id, user_id)
  - addComment(chart_id, comment)
  - recalculatePriority(chart_id)
  - getMetrics(chart_id)
```

### Business Rules at Chart Entered in EDC

```
Trigger: charts.status = 'entered_in_edc'
Actions:
1. Validate: user has 'mark_chart_entered' permission
2. Set entered_in_edc_date, entered_by
3. Calculate days_until_entry
4. Update chart_metrics
5. Complete related task
6. Recalculate analytics: average_entry_time, pending_charts
7. Write audit_log
```

### API Routes

```
GET    /api/charts
GET    /api/charts/queue
PATCH  /api/charts/:id/status
POST   /api/charts/:id/mark-entered
POST   /api/charts/:id/comments
```

### UI Pages

```
/app/(app)/charts/page.tsx     → Chart queue (grouped by site, priority-sorted)
/app/(app)/charts/[id]/page.tsx → Chart detail
```

### Tests

```
unit:   ChartService.test.ts (priority calculation, permission check)
int:    chart-entry.test.ts
e2e:    chart-entry.spec.ts
```

### Definition of Done

- Chart created automatically when visit is completed
- Chart queue sorted: Critical → Out of Window → Sponsor → Remaining
- Only authorized users can mark Entered in EDC
- CRC cannot edit EDC fields after Data Entry completes
- `entered_by`, `entered_at`, `days_until_entry` all recorded
- Audit Trail: chart created, chart entered
- Analytics updated: pending charts, average entry time

---

## Sprint 6 — Regulatory

**Goal:** Regulatory binder, document management, expiration tracking, health score.

### Database Migrations

```
006_regulatory_files.sql (partial — regulatory)
  - document_types
  - regulatory_binders
  - regulatory_documents
    → ADD binder_id FK ← (GAP-DB-04)
  - document_versions
  - document_history
  - study_document_requirements  → auto-populated on study activation
  - staff_documents
    → ADD study_id FK ← (GAP-DB-05)
```

### SQL Functions

```sql
document_health_score(study_id uuid, site_id uuid) → numeric
  Calculates: (current_required_docs / total_required_docs) * 100
  Deductions for: missing, expired, expiring_soon
```

### Edge Functions

```
supabase/edge-functions/regulatory-ai/index.ts
  → Receives: file_id
  → Runs Regulatory Agent prompt (extract doc type, version, expiration)
  → Stores result in: file_ai_metadata
  → Updates: regulatory_documents metadata
  → Returns: suggested metadata for review
```

### Services

```
RegulatoryService
  - getBinder(study_id, site_id)
  - uploadDocument(data)         → FileService.upload → RegulatoryAgent → pending review
  - archiveDocument(document_id)
  - addVersion(document_id, file_id)
  - getHealthScore(study_id, site_id)
  - checkExpirations()           → called by cron or BusinessRuleEngine
  - getStaffDocuments(user_id, site_id, study_id)
```

### Business Rules for Expiration

```
Trigger: daily check or document upload
Conditions: expiration_date within (90, 60, 30, 7) days OR already expired
Actions:
1. Update document.status → 'expiring_soon' or 'expired'
2. Create renewal task
3. Send notification
4. Write audit_log
```

### API Routes

```
GET    /api/regulatory/binders
GET    /api/regulatory/documents
POST   /api/regulatory/documents
POST   /api/regulatory/documents/:id/archive
POST   /api/regulatory/documents/:id/version
```

### UI Pages

```
/app/(app)/regulatory/page.tsx                   → Binder list
/app/(app)/regulatory/[study_id]/[site_id]/page.tsx → Regulatory binder
/app/(app)/regulatory/staff/page.tsx             → Staff documents
```

### Tests

```
unit:   RegulatoryService.test.ts (health score, expiration logic)
int:    regulatory-upload.test.ts
e2e:    regulatory-upload.spec.ts
```

### Definition of Done

- Upload document → AI extracts metadata → user reviews before saving
- Expiration alerts at 90/60/30/7 days and expired
- Document Health Score displayed
- Versioning: new version creates new file record (old preserved)
- Staff documents tracked per user/site/study
- Required document checklist auto-populated from `document_types.required_by_default`

---

## Sprint 7 — Enterprise Document Center

**Goal:** Central secure file repository with search, versioning, and AI metadata.

### Database Migrations

```
006_regulatory_files.sql (completing)
  - files
  - file_folders
  - file_links
  - file_versions
  - file_ai_metadata
    → search_vector tsvector ← (GAP-DB-06)

009_indexes_functions.sql
  - FTS indexes on files, subjects, studies
```

### Storage Buckets

```
subjects  → private
avatars   → private
reports   → private
exports   → private
ai-temp   → private, shorter expiry (24h)
```

### Edge Functions

```
supabase/edge-functions/document-ocr/index.ts
  → Receives: file_id
  → Uses Claude vision for OCR
  → Stores text in: file_ai_metadata.extracted_text
  → Updates: search_vector (tsvector for FTS)
```

### Services

```
FileService
  - upload(file, bucket, metadata)    → private, returns file_id
  - getSignedUrl(file_id, duration)   → 1h default, 24h for exports
  - linkToRecord(file_id, module, record_id)
  - search(query, filters)            → FTS on search_vector
  - aiExtract(file_id)               → calls OCR edge function
  - compareFiles(file_id_a, file_id_b) → AI comparison
  - createVersion(file_id, new_file)  → creates file_versions record
```

### API Routes

```
POST   /api/files/upload
GET    /api/files/:id
GET    /api/files/:id/signed-url
POST   /api/files/:id/link
GET    /api/files/search
POST   /api/files/:id/ai-extract
POST   /api/files/compare
```

### UI Pages

```
/app/(app)/documents/page.tsx       → Document center (list + grid view)
/app/(app)/documents/[id]/page.tsx  → Document detail with preview panel
```

### Definition of Done

- All file uploads go through FileService (no direct storage access)
- All files have private storage paths
- Signed URLs generated on request with expiry
- OCR extracts text from uploaded documents
- Full-text search works across file names and extracted text
- Version history shows all versions of a document
- No permanent public URLs anywhere

---

## Sprint 8 — Task Engine

**Goal:** Complete operational task work queue with real-time updates.

### Database Migrations (completing 005)

```
005_charts_tasks_analytics.sql (completing)
  - tasks
  - task_history
  - task_comments

008_notifications.sql  ← (GAP-DB-01)
  - notifications
  - notification_preferences
```

### Realtime Setup

```
Channels:
  company:{company_id}:tasks
  company:{company_id}:notifications

Subscriptions:
  client subscribes on login
  receives INSERT events on tasks where assigned_to = current_user
```

### Edge Functions

```
supabase/edge-functions/task-generator/index.ts
  → Called by: service layer events (visit completed, doc expiring, etc.)
  → Creates task record
  → Sends realtime event

supabase/edge-functions/notification-dispatcher/index.ts
  → Reads notification_preferences for affected users
  → Sends in-app notification (writes to notifications table)
  → Sends email via provider
```

### Services

```
TaskService
  - createTask(data)            → called by BusinessRuleEngine
  - completeTask(task_id)       → may trigger downstream effects
  - reassignTask(task_id, user_id)
  - addComment(task_id, comment)
  - getMyToday(user_id)         → tasks due today for current user
  - getQueue(site_id, filters)  → all tasks ordered by priority

NotificationService
  - notify(user_id, type, payload)
  - getUnread(user_id)
  - markRead(notification_id)
  - getUserPreferences(user_id)
  - updatePreferences(user_id, prefs)
```

### API Routes

```
GET    /api/tasks
GET    /api/tasks/my-today
PATCH  /api/tasks/:id/status
POST   /api/tasks/:id/comments
POST   /api/tasks/:id/complete
```

### UI Pages

```
/app/(app)/task-center/page.tsx    → Task center (grouped by site, priority-sorted)
```

### Definition of Done

- Task Center shows all tasks grouped by site, sorted by priority
- Task completion triggers downstream effects (chart update, etc.)
- Real-time: new tasks appear without page refresh
- In-app notifications visible in TopBar bell
- Email notifications sent for critical events
- Admin can reassign tasks

---

## Sprint 9 — Business Rules Engine

**Goal:** Configurable rule builder and execution engine.

### Business Rule JSON Schema (GAP-BL-01 Resolution)

```typescript
// conditions schema
type RuleCondition = {
  field: string; // e.g., "chart.days_pending"
  operator: '>' | '<' | '>=' | '<=' | '==' | '!=' | 'in' | 'not_in';
  value: number | string | string[];
  logic?: 'AND' | 'OR'; // for combining multiple conditions
};

// actions schema
type RuleAction = {
  type:
    | 'create_task'
    | 'update_status'
    | 'send_notification'
    | 'create_chart'
    | 'calculate_kpi'
    | 'write_audit';
  payload: Record<string, unknown>;
};
```

### Services

```
BusinessRuleEngine
  - evaluate(trigger: string, context: Record<string, unknown>)
    → finds active rules matching trigger
    → evaluates conditions against context
    → executes actions
    → logs to rule_execution_logs

RuleEvaluator
  - evaluateCondition(condition: RuleCondition, context)  → boolean
  - executeAction(action: RuleAction, context)            → void
```

### API Routes

```
GET    /api/business-rules
POST   /api/business-rules
PATCH  /api/business-rules/:id
POST   /api/business-rules/:id/test
POST   /api/business-rules/:id/execute
```

### UI Pages

```
/app/(app)/settings/business-rules/page.tsx → Rule list
```

### Default Rules (seed data)

```
- chart_priority_critical:   chart.days_pending > 7 → priority = critical
- chart_priority_high:       chart.days_pending in [4..7] → priority = high
- doc_expiring_90:           doc.expiration_date within 90 days → create_renewal_task
- doc_expiring_30:           doc.expiration_date within 30 days → notify_regulatory
- visit_out_of_window:       visit.completion_date outside window → increase_priority + notify_crc
- subject_early_term:        subject.status = early_terminated → cancel_future_visits
- study_closeout:            study.status = closed → archive_binder + generate_closeout_tasks
```

### Definition of Done

- Admin can view, create, edit, disable rules via UI
- Engine evaluates correct rules on every trigger
- All rule executions logged in `rule_execution_logs`
- Default system rules run automatically
- Admin can test a rule against sample data before activating
- System failure during rule execution: logs failure, notifies admin

---

## Sprint 10 — Clinical Intelligence

**Goal:** AI agents integrated across Zentariq One with approval workflows.

### AI Provider Client

```
lib/ai/provider.ts
  - Class: AIProviderClient
  - Methods: analyze(system_prompt, user_prompt, output_schema)
  - Provider: Claude claude-sonnet-4-6
  - Retry: 3 attempts, exponential backoff
  - Logging: all requests to ai_requests table
  - OCR: Claude vision (GAP-AI-02)
```

### Services

```
ClinicalIntelligenceService
  - runProtocolAgent(file_id, study_id)
  - runRegulatoryAgent(file_id)
  - runSubjectAgent(subject_id)
  - runDataAgent(site_id)
  - runAnalyticsAgent(company_id)
  - runExecutiveAgent(company_id)
  - runCopilot(message, context)
  - getPendingSuggestions(user_id)
  - approveSuggestion(ai_response_id)
  - rejectSuggestion(ai_response_id)
```

### Edge Functions (refinements)

```
protocol-ai   → calls ClinicalIntelligenceService.runProtocolAgent
regulatory-ai → calls ClinicalIntelligenceService.runRegulatoryAgent
```

### API Routes

```
POST   /api/ai/protocol/analyze
POST   /api/ai/regulatory/analyze
POST   /api/ai/subject/review
POST   /api/ai/data/review-chart
POST   /api/ai/analytics/ask
POST   /api/ai/copilot
POST   /api/ai/suggestions
```

### UI Pages

```
/app/(app)/clinical-intelligence/page.tsx        → Agent overview
/app/(app)/clinical-intelligence/copilot/page.tsx
/app/(app)/clinical-intelligence/suggestions/page.tsx → Pending reviews
```

### Definition of Done

- Protocol Agent: upload PDF → draft study + visit template
- Regulatory Agent: upload doc → metadata extracted → pending review
- Subject Agent: reviews subject and surfaces issues
- Data Agent: queue prioritization visible
- Copilot: persistent panel available on all pages
- All AI requests logged
- No AI suggestion modifies production data without user approval
- AI respects company/site access boundaries

---

## Sprint 11 — Analytics

**Goal:** Executive and operational dashboards, reports, KPI drill-downs, exports.

### Database Migrations (completing 005)

```
005_charts_tasks_analytics.sql (completing)
  - analytics_kpis
  - saved_reports
  - saved_filters
  - report_exports
```

### Edge Functions

```
supabase/edge-functions/analytics-summary/index.ts
  → Aggregates KPIs from operational tables
  → Called: on demand or via cron (hourly)
  → Updates: analytics_kpis
```

### Services

```
AnalyticsService
  - getDashboard(user_id, role)       → role-specific KPI set
  - getKPIs(company_id, site_id)
  - getReports(company_id, filters)
  - exportReport(report_type, filters) → creates file via FileService
  - saveReport(user_id, config)
  - recalculate(kpi_name, context)    → called from all services
```

### API Routes

```
GET    /api/analytics/dashboard
GET    /api/analytics/kpis
GET    /api/analytics/reports
POST   /api/analytics/reports/export
POST   /api/analytics/saved-reports
```

### UI Pages

```
/app/(app)/analytics/page.tsx
/app/(app)/analytics/executive/page.tsx
/app/(app)/analytics/operational/page.tsx
```

### Definition of Done

- Dashboard shows role-specific KPIs
- Every KPI card is clickable (opens underlying records)
- CEO sees executive summary with AI narrative
- Reports exportable as PDF, Excel, CSV
- Saved reports work per user
- KPIs updated incrementally (not full recalculation on every view)

---

## Sprint 12 — Settings

**Goal:** Complete settings module — company, users, roles, document types, AI config.

### UI Pages

```
/app/(app)/settings/company/page.tsx
/app/(app)/settings/users/page.tsx      (enhance from Sprint 1)
/app/(app)/settings/roles/page.tsx
/app/(app)/settings/sites/page.tsx      (enhance from Sprint 1)
/app/(app)/settings/document-types/page.tsx
/app/(app)/settings/business-rules/page.tsx (enhance from Sprint 9)
/app/(app)/settings/notifications/page.tsx
/app/(app)/settings/ai-configuration/page.tsx
```

### Definition of Done

- All settings sections function correctly
- All changes create Audit Trail records
- Only Admin role can access settings
- Document type expiration rules configurable per type
- AI agents can be enabled/disabled per company

---

## Sprint 13 — Security Hardening

**Goal:** Complete security audit, RLS review, rate limiting, pen test checklist.

### Tasks

```
1. Review every RLS policy for correctness
2. Test cross-company isolation (automated test suite)
3. Test site isolation (automated test suite)
4. Implement API rate limiting (Vercel Edge + Upstash Redis)
5. Implement AI request rate limiting per user/day
6. Enable Supabase MFA enrollment (optional for users)
7. Audit all signed URL configurations
8. Verify SUPABASE_SERVICE_ROLE_KEY is never exposed in client code
9. Validate all API inputs with Zod schemas
10. Review error messages (no SQL/stack traces exposed)
11. Test file access authorization (signed URL requires valid session)
12. Security monitoring: track failed logins in audit_logs
```

### Tests

```
security: rls-isolation.test.ts (comprehensive)
security: api-authorization.test.ts
security: file-access.test.ts
```

### Definition of Done

- Zero cross-company data exposure (automated tests pass)
- All API endpoints require authentication
- Rate limiting active on auth and AI endpoints
- No service role key in any client-side code
- All inputs validated server-side

---

## Sprint 14 — Production Readiness

**Goal:** Documentation, monitoring, backup verification, final QA, production deployment.

### Tasks

```
1. Verify Supabase daily backup is enabled
2. Set up error tracking (Sentry or Vercel error monitoring)
3. Set up performance monitoring (Vercel Analytics)
4. Run complete Playwright e2e suite
5. Verify all audit log entries for every sprint's critical workflows
6. Verify Vercel deployment rollback works
7. Final RLS and permission validation
8. Load test: simulate 100 concurrent users
9. Verify all environment variables are set in Vercel
10. Documentation review
```

### Definition of Done

- All 14 sprints' Definition of Done criteria verified
- E2E tests: 100% pass
- No critical errors in Sentry
- Production deployment successful
- Rollback tested
- Backup verified with point-in-time restore test

---

## Sprint Velocity Guidelines

Each sprint is designed to be completable in approximately **2-3 weeks** of focused development, given Claude Code assistance.

The sprint sequence must not be reordered. Dependencies between sprints are hard.

Sprints 1-5 are the critical path. Delays here cascade to all subsequent sprints.
